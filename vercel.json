{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node",
      "config": {
        "NODE_OPTIONS": "--require tsconfig-paths/register"
      }
    }
  ],
  "routes": [
    { "src": "/.*", "dest": "src/index.ts" }
  ]
}
